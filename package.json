{
  "name": "employee-management-system",
  "version": "1.0.0",
  "description": "Employee Management System with Firebase",
  "scripts": {
    "deploy": "firebase deploy --only hosting",
    "serve": "firebase serve"
  },
  "dependencies": {
    "firebase": "^10.7.0"
  }
}
